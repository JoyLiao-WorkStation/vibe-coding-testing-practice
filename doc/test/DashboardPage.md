---
description: DashboardPage 測試案例清單
---

> 狀態：初始為 [ ]、完成為 [x]
> 注意：狀態只能在測試通過後由流程更新。

---

## [x] 【前端元素】顯示儀表板基本元件
**範例輸入**：無  
**期待輸出**：畫面頂部顯示「儀表板」標題，並有歡迎文字區塊與「登出」按鈕。

---

## [x] 【前端元素】正確顯示使用者名稱與字母頭像
**範例輸入**：`useAuth` 回傳的 `user` 物件中 `username` 為 `'TestUser'`。  
**期待輸出**：畫面顯示「Welcome, TestUser 👋」，且頭像區塊顯示大寫字母 `'T'`。

---

## [x] 【前端元素】管理員顯示管理後台連結
**範例輸入**：`useAuth` 回傳的 `user` 物件中 `role` 為 `'admin'`。  
**期待輸出**：畫面上方導覽列顯示「🛠️ 管理後台」連結。

---

## [x] 【前端元素】一般用戶不顯示管理後台連結
**範例輸入**：`useAuth` 回傳的 `user` 物件中 `role` 為 `'user'`。  
**期待輸出**：畫面上方導覽列不應該顯示「🛠️ 管理後台」連結。

---

## [x] 【Mock API】載入中狀態顯示
**範例輸入**：`productApi.getProducts` API 處於請求中 (Pending) 狀態。  
**期待輸出**：商品列表區塊顯示「載入商品中...」與讀取動畫。

---

## [x] 【Mock API】成功載入商品列表
**範例輸入**：`productApi.getProducts` 回傳含有數個商品資料的陣列。  
**期待輸出**：畫面成功渲染出各個商品的名稱、描述與價格。

---

## [x] 【Mock API】載入商品失敗顯示錯誤訊息
**範例輸入**：`productApi.getProducts` 拋出錯誤，且 status 不是 401，並夾帶錯誤訊息 `'伺服器錯誤'`。  
**期待輸出**：畫面上不顯示商品列表，而是顯示錯誤圖示與文字「伺服器錯誤」。

---

## [x] 【Mock API】載入商品 401 錯誤處理
**範例輸入**：`productApi.getProducts` 拋出錯誤，且 status 為 401。  
**期待輸出**：元件內的 error 狀態不會被設定為 API 的錯誤訊息 (因為交由 interceptor 處理)，畫面上不顯示 API 的錯誤文字。

---

## [x] 【function 邏輯】點擊登出
**範例輸入**：點擊右上角的「登出」按鈕。  
**期待輸出**：正確呼叫 `logout` 函數，並且使用 `navigate` 將路由取代 (`replace: true`) 至 `/login`。
