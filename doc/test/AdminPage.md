---
description: AdminPage 測試案例清單
---

> 狀態：初始為 [ ]、完成為 [x]
> 注意：狀態只能在測試通過後由流程更新。

---

## [x] 【前端元素】顯示管理後台基本介面
**範例輸入**：無  
**期待輸出**：畫面正確顯示「🛠️ 管理後台」標題、「返回」連結以及管理員專屬的資訊卡片。

---

## [x] 【前端元素】根據 role 顯示管理員標籤
**範例輸入**：`useAuth` 回傳的 `user` 物件中 `role` 為 `'admin'`。  
**期待輸出**：畫面上方的 role badge 顯示文字為「管理員」。

---

## [x] 【前端元素】根據 role 顯示一般用戶標籤
**範例輸入**：`useAuth` 回傳的 `user` 物件中 `role` 為 `'user'`。  
**期待輸出**：畫面上方的 role badge 顯示文字為「一般用戶」。

---

## [ ] 【function 邏輯】點擊登出
**範例輸入**：點擊右上角的「登出」按鈕。  
**期待輸出**：正確呼叫 `logout` 函數，並且使用 `navigate` 將路由取代 (`replace: true`) 至 `/login`，同時清除 state。
